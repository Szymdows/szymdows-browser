[package]
name = "html_parser"
version = "0.1.2"
edition = "2021"

[lib]
# This tells Rust to build both a Rust library and a C-compatible library
crate-type = ["cdylib", "rlib"]

[dependencies]